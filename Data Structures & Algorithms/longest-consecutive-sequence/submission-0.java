class Solution {
    public int longestConsecutive(int[] nums) {
        Map<Integer, Integer> sequence = new HashMap<>():
        int length = 0; 
        Arrays.sort(nums);
        
        for( int idx = 0; idx < nums.length; idx++ ) {
            sequence.put(nums[idx], idx); 
            if( nums[idx] != sequence.get(nums[idx])  )
            if curr elem 5 is not last elem n + 1
             then 
        }
    }
}
