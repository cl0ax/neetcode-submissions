class Solution {
    public int maxArea(int[] heights) {
        int left = 0;
        int maxArea = 1; 
        for(int right = 0; right < heights.length; right++){
            currArea = 
            maxArea = Math.max(max, )
        }
    }
}
