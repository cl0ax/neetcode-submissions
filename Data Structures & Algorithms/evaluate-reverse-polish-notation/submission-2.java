class Solution {
    public int evalRPN(String[] tokens) {
        Deque<Integer> stack = new DequeArray<>();

        for(int i = 0; i < tokens.length; i++){
            String str = tokens[i];
            if()
        }

    }
}
